1、 二分查找的模板(最基本的)

```
while (left <= right) {
  int mid = left + (right - left) / 2;
  if (buf[mid] == target) {
    return ;
  } else if (buf[mid] > target) {
    right = mid - 1;
  } else if (buf[mid] < target) {
    left = mid + 1;
  }
}
```
